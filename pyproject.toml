[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "athene"
version = "0.1.0"
description = "Minimalist ALC description logic tableau reasoner"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["."]
include = ["reasoner*"]
